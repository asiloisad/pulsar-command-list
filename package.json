{
  "name": "command-list",
  "description": "Find and run available commands with fuzzy search",
  "repository": "https://github.com/asiloisad/pulsar-command-list",
  "version": "0.1.2",
  "license": "MIT",
  "engines": {
    "atom": ">=1.100.0 <2.0.0"
  },
  "main": "./lib/main",
  "activationCommands": {
    "atom-workspace": [
      "command-list:toggle",
      "command-list:show-hidden-commands"
    ]
  },
  "configSchema": {
    "recentCount": {
      "order": 1,
      "title": "Number of Recent Items",
      "description": "How many recently used commands to keep at the top of the list.",
      "type": "integer",
      "default": 10,
      "minimum": 0
    },
    "preserveQuery": {
      "order": 2,
      "title": "Preserve Query",
      "description": "Keep the search query when reopening the command list.",
      "type": "boolean",
      "default": false
    }
  },
  "dependencies": {
    "pulsar-select-list": "^1.4.1"
  }
}
